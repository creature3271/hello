function showRadius(circle) {
    let message = prompt('Radius');

    let areaOfCircle = (message * message * 3.14159265359);

    alert(areaOfCircle);
}

showRadius(prompt);
//write a javascript page that prompts the user for the radius and then returns the area of a circle
//to find the area of a circle - pi x radius squared